{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "echo '⚠ SELF-CHECK: Did you search for existing implementations before writing this? Does this follow DI patterns (Autofac modules, not manual wiring)? Check .claude/rules/ for conventions.'"
          }
        ]
      }
    ]
  }
}
