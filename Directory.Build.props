<Project>

  <PropertyGroup>
    <Configuration Condition="'$(Configuration)' == ''">Debug</Configuration>
    <ContinuousIntegrationBuild Condition="'$(CI)' == 'true'">true</ContinuousIntegrationBuild>
    <DefineConstants Condition="'$(EnableZkEvm)' == 'true'">$(DefineConstants);ZK_EVM</DefineConstants>
    <InvariantGlobalization>true</InvariantGlobalization>
    <LangVersion>14.0</LangVersion>
    <SatelliteResourceLanguages>en</SatelliteResourceLanguages>
    <TargetFramework>net10.0</TargetFramework>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <!-- NU1900: vulnerability metadata fetch failure — don't break builds when a feed is down -->
    <NoWarn>$(NoWarn);NU1900</NoWarn>
    <UseArtifactsOutput>true</UseArtifactsOutput>
  </PropertyGroup>

  <PropertyGroup Label="ProductInfo">
    <Copyright>Demerzel Solutions Limited</Copyright>
    <Product>Nethermind</Product>
  </PropertyGroup>

</Project>
