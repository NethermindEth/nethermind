# SLOAD/SSTORE handler extraction scope

This extractor closes the standard-mainnet Amsterdam route from `IVirtualMachine` registration through `EthereumVirtualMachine`, the eight exact SLOAD/SSTORE roots in the four tracing/cancellation tables, their wrappers, and the joint EIP-2200/2929/8037/8038 handler bodies. It admits the complete bytes of 55 production and build sources, including every named-mainnet ancestor. Regeneration cannot change the reviewed admission hashes.

The theorem-free generated Lean model is independent of `Eip803x.Evm.PersistentStorage` and `Eip803x.SStore`. Its separate refinement maps the generated cells, slots, access state, pricing effect, events, and handler state to those handwritten models. The pinned claim applies only to normal consensus execution with `VM.IsTracingAccess = false`; access-list tracing deliberately follows a distinct warm-cost path and is not modeled by this refinement. Within that premise, the claim covers opcode identity, the exact Amsterdam specializations, PC advance, static and stipend checks, sequential stack effects, cold/warm charging and warming, current/original reads, the EIP-8038 price, execution-before-state charging, refund/refill order, failure states, and write-on-success behavior.

The admitted handler composes the separately extracted and proved SSTORE pricing, state-charge, and state-refill kernels. This leaf does not re-prove their fixed-width implementations. `IWorldState` provider behavior, storage journaling and rollback, database persistence, byte/`UInt256`/stack representation, access-tracker collection implementation, access-list tracing, tracing callbacks and metrics, cancellation, CLR/JIT and unsafe function-pointer execution, transaction settlement, receipts, and block accounting remain exact, ordinally validated obligations in the IR.
