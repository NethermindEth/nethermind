{"body":"**Claude finished @Marchhill's task in 9m 11s** —— [View job](https://github.com/NethermindEth/nethermind/actions/runs/26510325410)\n\n---\n### EIP-7805 (FOCIL) PR Review\n\n**Tasks:**\n- [x] Load repo rules and context\n- [x] Read key changed files\n- [x] Review correctness and consensus logic\n- [x] Review security implications\n- [x] Review performance (hot paths)\n- [x] Review adherence to repo coding standards\n- [x] Post inline comments\n- [x] Write summary\n\n---\n\n### Summary\n\nThe overall structure is solid: a clean fork spec (`Bogota`), well-scoped Engine API additions (`engine_getInclusionListV1`, `engine_newPayloadV6`, `engine_forkchoiceUpdatedV5`), a correct block-production pipeline that prepends IL transactions ahead of the mempool, and a clear signal flow from `ProcessOne` through `BlockchainProcessor` to the `INVALID_INCLUSION_LIST` RPC response. The test coverage for the validator and engine-module E2E paths is good.\n\nThere are three issues that need to be resolved before merge:\n\n---\n\n### High\n\n**1. Parallel reads on a non-thread-safe `IWorldState`** — [`InclusionListValidator.cs:39`](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3310726853)\n\n`CouldIncludeTx` calls `worldState.GetBalance` / `worldState.GetNonce` in parallel via `.AsParallel().Any(...)`. `StateProvider` internally traverses a Patricia trie with mutable caches that are not protected by any lock. `BlockCachePreWarmer` — the repo's reference for parallel state access — explicitly isolates each worker behind a read-only scope obtained from `_txProcessingEnvPool`. The validator reuses the shared main-chain `WorldState` with no such isolation. The straightforward fix is to drop `.AsParallel()` here; parallelism can be added later via a read-only pool if profiling shows it matters.\n\n**2. Post-execution state deviates from EIP-7805 spec** — [`InclusionListValidator.cs:46`](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3310727617)\n\nEIP-7805 says a builder violates the IL if T \"must not be valid when applied to the **state of the parent block**\" (pre-execution). The code checks against the post-execution world state. A non-IL transaction from the same sender as an IL transaction can advance the sender's nonce, making the post-execution nonce check return `false` for the IL transaction — the IL appears satisfied when it shouldn't be. Marchhill's justification (\"only the IL tx alters state\") does not cover this cross-sender scenario. The fix is to snapshot the pre-execution state root before `ProcessBlock` and run the IL check against a read-only view at that root.\n\n**3. Null `SenderAddress` dereference in `CouldIncludeTx`** — [`InclusionListValidator.cs:54`](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3310727851)\n\nIf signature recovery fails for a malformed IL transaction, `tx.SenderAddress` is `null`. `worldState.GetBalance(null)` / `GetNonce(null)` will throw. A `null` guard is needed before the world-state calls.\n\n---\n\n### Medium\n\n**4. `BlockProductionTransactionsExecutor.IsTransactionInBlock` throws `NotImplementedException`** — [`BlockProductionTransactionsExecutor.cs:58`](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3310729141)\n\nNot triggered today (production blocks have `InclusionListTransactions == null`, so the LINQ short-circuits), but it is a landmine. The large block of commented-out constructors above it also suggests incomplete refactoring that should be resolved.\n\n**5. `InclusionListTxSource` has no memory barrier between RPC thread and producer thread** — [`InclusionListTxSource.cs:18`](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3310729638)\n\n`_inclusionListTransactions` is written from the RPC thread and read from the block-producer thread with no `volatile`, `Interlocked`, or lock. The field is also never cleared between slots, so a stale IL from a previous slot persists into the next block-production cycle when no fresh FCU is received.\n\n**6. Empty IL in FCU silently leaves stale IL active** — [`EngineRpcModule.Bogota.cs:48`](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3310730877)\n\n`is { Length: \u003e 0 }` skips the `Set()` call for an empty array. An empty IL from the CL is a valid signal that should clear the previous slot's IL. The guard should be `is { } ilTxs` (not-null check only).\n\n---\n\n### Low\n\n- Large commented-out dead code in `RecoverSignatures.cs` lines 46–109 — [`RecoverSignatures.cs:46`](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3310731385)\n- `IInclusionListValidator` XML doc: `\u003cparam name=\"inclusionListTransactions\"\u003e` doesn't match the actual `block` parameter — [`IInclusionListValidator.cs:21`](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3310732742)\n- `InvalidInclusionList` not stored in `_latestBlocks` (probably intentional, but worth a comment) — [`NewPayloadV1Result.cs:25`](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3310733456)\n- IL transactions decoded twice on the `newPayload` path: once in `ExecutionPayloadV3.TryGetBlock` (RLP decode) and again implicitly in `RecoverSignatures.RecoverData`. Signatures are recovered a second time in both the `forkchoiceUpdated` (via `InclusionListDecoder`) and `newPayload` (via `RecoverSignatures` step) paths — `??=` prevents a hard error but wastes CPU on the validation hot path.","date":"2026-05-27T12:13:46Z"}
{"body":"**Claude finished @Marchhill's task in 9m 26s** —— [View job](https://github.com/NethermindEth/nethermind/actions/runs/26530319488)\n\n---\n**Claude finished @Marchhill's task** — \n\n---\n\n### EIP-7805 (FOCIL) PR Review — Round 2\n\n**Tasks:**\n- [x] Load repo rules and context\n- [x] Read key changed files\n- [x] Review correctness and consensus logic\n- [x] Review security implications\n- [x] Review performance (hot paths)\n- [x] Review adherence to repo coding standards\n- [x] Post inline comments\n- [x] Write summary\n\n---\n\n### Progress since last review\n\nThe previous round's six findings have been addressed cleanly:\n\n| Previous finding | Status |\n|---|---|\n| `.AsParallel()` race on `IWorldState` (High) | ✅ Fixed — serial scan, class-level remarks explain why |\n| `null SenderAddress` NRE in `CouldIncludeTx` (High) | ✅ Fixed — null guard returns `false` |\n| `volatile` missing on `_inclusionListTransactions` (Medium) | ✅ Fixed — `Volatile.Read`/`Write` |\n| Empty IL skips `Set()` leaving stale IL (Medium) | ✅ Fixed — guard changed to `is { }` |\n| `NotImplementedException` in `IsTransactionInBlock` (Medium) | ✅ Fixed — returns `false`, well-commented |\n| Commented-out dead code in `RecoverSignatures.cs` (Low) | ✅ Fixed — removed |\n\n---\n\n### Remaining issues\n\n#### High\n\n**Post-execution state for IL validity check (spec deviation / censorship bypass)**  \n→ [`InclusionListValidator.cs:79`](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3312997433)\n\n`CouldIncludeTx` reads `worldState.GetNonce` / `worldState.GetBalance` from the fully committed post-execution state (all block transactions applied). EIP-7805 §Execution Layer specifies that the \"is this tx appendable?\" check must use **parent block state `S`**, updated only by IL transactions already in the block — non-IL transactions in the block do not affect `S`.\n\nThis creates an exploitable gap: a builder who wants to censor IL transaction `T` from sender `S` (nonce 5) can include a different signed transaction `N` from `S` with nonce 5 (a replacement). Post-execution `S.nonce = 6`; `CouldIncludeTx(T)` returns `false`; the validator reports IL satisfied. But at the parent state `S.nonce = 5 = T.nonce`, so `T` was valid and the spec requires `INVALID_INCLUSION_LIST`. The argument \"only IL txs alter state\" does not hold when a non-IL tx from the same sender is in the block.\n\nThe fix requires running the IL check against a snapshot of the pre-execution state root, not the live post-execution worldstate. One approach: capture `stateProvider.StateRoot` before `ProcessBlock`, open a read-only snapshot at that root, and pass it to the validator.\n\n#### Medium\n\n**`catch (System.Exception)` swallows all exceptions including bugs**  \n→ [`EngineRpcModule.Bogota.cs:63`](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3312998733)\n\nThe repo robustness rule explicitly states: *\"Never swallow exceptions in an empty catch block — at minimum log the exception.\"* The current empty catch on `inclusionListTxSource.Set(...)` will silently eat `NullReferenceException`, `OutOfMemoryException`, and any future regression in the decode path. At minimum, log at `_logger.Warn`. Ideally narrow to specific decoding exceptions (`RlpException | ArgumentException | IndexOutOfRangeException`).\n\n#### Low\n\n**Commented-out constructor bodies in `BlockProductionTransactionsExecutor.cs`**  \n→ [`BlockProductionTransactionsExecutor.cs:34–56`](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3312999546)\n\nTwo constructor bodies are still commented out. Per repo guidelines, dead code should be deleted, not commented out.\n\n---\n\n**Verdict: not yet mergeable.** The post-execution state issue (High) leaves the core FOCIL censorship-resistance guarantee unimplemented per spec, and the silent exception swallow (Medium) violates a documented repo robustness rule.","date":"2026-05-27T18:23:52Z"}
{"body":"**Claude finished @Marchhill's task in 6m 27s** —— [View job](https://github.com/NethermindEth/nethermind/actions/runs/26576129818)\n\n---\n**Claude finished @Marchhill's task** —\n\n---\n\n### EIP-7805 (FOCIL) PR Review — Round 3\n\n**Tasks:**\n- [x] Load repo rules and context\n- [x] Read key changed files (focusing on changes since last review)\n- [x] Check if previous High/Medium findings are resolved\n- [x] Post inline comments\n- [x] Write summary\n\n---\n\n### Progress since Round 2\n\nThe latest commit (`0dc087d0 Simplify IL validator and narrow FCUv5 catch`) resolved all three Round-2 Medium/Low issues cleanly:\n\n| Round 2 finding | Status |\n|---|---|\n| `catch (System.Exception)` swallows bugs (Medium) | ✅ Fixed — narrowed to `RlpException \\| ArgumentException \\| IndexOutOfRangeException`, logged at Debug |\n| Commented-out constructor bodies in `BlockProductionTransactionsExecutor` (Low) | ✅ Removed |\n| `NotImplementedException` in `IsTransactionInBlock` (Low) | ✅ Resolved by removing the interface member entirely; validator now uses `block.Transactions` directly via a `HashSet\u003cTransaction\u003e` |\n\n---\n\n### Remaining / new issues\n\n#### High\n\n**Post-execution state for IL validity check (spec violation / censorship bypass)** — [`InclusionListValidator.cs:81`](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3317951770)\n\nCarried from Round 2. The `BlockProcessor.cs` comment now explicitly acknowledges the design: `\"the validator reads sender balance/nonce from the post-execution state\"`. The spec requires the opposite.\n\nEIP-7805 §Execution Layer defines state `S` as the **parent block state**, updated only by IL transactions that appear in the block. Non-IL transactions must **not** affect `S`.\n\nThe exploit is straightforward: include a signed replacement transaction `N` from the same sender as IL tx `T` (same nonce, different payload). `N` executes → sender nonce advances → `CouldIncludeTx(T)` returns `false` → validator declares IL satisfied. At the parent state, `T` was perfectly valid → spec requires `INVALID_INCLUSION_LIST`. FOCIL's censorship resistance guarantee is bypassed.\n\nThe worldstate scope constraint (state must be accessed inside the BranchProcessor scope) is a real engineering challenge, but it doesn't justify the spec deviation. The fix path is to snapshot the pre-execution state root before `ProcessBlock` and open a read-only view at that root for the IL check — the same pattern already used by read-only callers elsewhere in the codebase.\n\n#### Medium\n\n**EIP-1559 `GasPrice` vs `MaxFeePerGas` in `CouldIncludeTx`** — [`InclusionListValidator.cs:82`](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3317953172)\n\nNew in Round 3. For a type-2 (EIP-1559) transaction, `tx.GasPrice` is `maxPriorityFeePerGas`; the validity-relevant cap is `tx.MaxFeePerGas`. `TransactionProcessor.cs:787` correctly uses `MaxFeePerGas` for the baseFee check. The IL validator uses `GasPrice`, so any EIP-1559 IL tx with `maxPriorityFeePerGas \u003c baseFee` (common in high-fee environments) is incorrectly deemed \"not appendable\" even if `maxFeePerGas \u003e= baseFee`. The balance/cost calculation has the same problem (`tx.GasLimit * tx.GasPrice` instead of `tx.GasLimit * tx.MaxFeePerGas`). Both checks should use `tx.MaxFeePerGas`.\n\n---\n\n**Verdict: not yet mergeable.** The post-execution state issue (High) remains the core FOCIL censorship-resistance gap from Round 2, and the EIP-1559 fee check (Medium) is a new correctness bug that could allow legitimate EIP-1559 IL transactions to be bypassed.","date":"2026-05-28T12:58:44Z"}
{"body":"**Claude finished @Marchhill's task in 5m 1s** —— [View job](https://github.com/NethermindEth/nethermind/actions/runs/26580012292)\n\n---\n**Claude finished @Marchhill's task** —\n\n---\n\n### EIP-7805 (FOCIL) PR Review — Round 4\n\n**Tasks:**\n- [x] Load repo rules and context\n- [x] Read key changed files (focusing on Round 3 unresolved issues)\n- [x] Verify parent-state fix for IL validity check\n- [x] Verify MaxFeePerGas fix\n- [x] Post inline comments\n- [x] Write summary\n\n---\n\n### Progress since Round 3\n\nBoth outstanding findings from Round 3 have been resolved cleanly:\n\n| Round 3 finding | Status |\n|---|---|\n| Post-execution state for IL validity check (High) | ✅ Fixed — `BlockProcessor.CaptureParentSenderState()` snapshots nonce/balance before `ProcessBlock()`; `ilDeltas` accumulates effects of included IL txs per spec; regression test `Same_nonce_replacement_tx_does_not_let_builder_skip_il_tx` covers the censorship attack |\n| EIP-1559 `GasPrice` vs `MaxFeePerGas` (Medium) | ✅ Fixed — `CouldIncludeTx` now uses `tx.MaxFeePerGas` for both the base-fee check and cost computation; regression test `Eip1559_tx_with_low_tip_but_sufficient_fee_cap_is_appendable` covers the case |\n\nThe parent-state design is sound:\n- `CaptureParentSenderState` reads from `_stateProvider` before `ProcessBlock` mutates it — timing is correct\n- `ilDeltas` correctly models \"parent state + included IL txs\" as the EIP-7805 reference state `S`\n- `tx.MaxFeePerGas` resolves to `GasPrice` for legacy type-0 txs (`Transaction.cs:51`), so the fix is correct for all tx types\n\n---\n\n### Remaining issues\n\n#### Low\n\n**Commented-out dead code in `BlockchainProcessor.cs`** → [discussion_r3318580344](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3318580344)\n\nLines 339–370: ~30 lines of commented-out old processing loop. Per repo guidelines, remove rather than comment out.\n\n**Stale doc comment in `EngineRpcModule.Bogota.cs:33`** → [discussion_r3318580808](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3318580808)\n\nThe `engine_newPayloadV6` summary still says \"validates against the **post-execution state**\". After the Round 3 fix the implementation validates against the **parent block state** — a one-word suggestion is attached.\n\n---\n\n**Verdict: mergeable.** All Critical/High/Medium findings are resolved. The two remaining Low items are cosmetic (dead code, stale comment) and do not block merge.","date":"2026-05-28T14:35:44Z"}
{"body":"**Claude finished @Marchhill's task in 4m 24s** —— [View job](https://github.com/NethermindEth/nethermind/actions/runs/26584185888)\n\n---\n### EIP-7805 (FOCIL) PR Review — Round 5\n\n**Tasks:**\n- [x] Load repo rules and context\n- [x] Read key changed files (focusing on Round 4 Low findings and new commits)\n- [x] Verify Round 4 Low findings are resolved (commented-out code, stale doc comment)\n- [x] Check for any new issues introduced since Round 4\n- [x] Post inline comments\n- [x] Write summary\n\n---\n\n### Progress since Round 4\n\nBoth Round 4 Low findings have been addressed cleanly (commit `40bce39e`):\n\n| Round 4 finding | Status |\n|---|---|\n| Commented-out dead code in `BlockchainProcessor.cs` lines 339–370 (Low) | ✅ Deleted |\n| Stale doc comment in `EngineRpcModule.Bogota.cs` (\"post-execution state\") (Low) | ✅ Updated to \"parent block state (snapshotted before processing the block)\" |\n\n---\n\n### New commits since Round 4\n\nCommit `4a4f95b4 IL: combine decode + signature recovery into one parallel pass` folds decode and ECDSA recovery into a single `ParallelUnbalancedWork.For` dispatch in `InclusionListDecoder`, reducing the FCUv5 hot-path from two `Parallel.For` dispatches to one. The change also lowers the parallel threshold from 8 to 4 (appropriate for ILs bounded at 16 txs by `Eip7805Constants`), and the `RecoverSignatures.RecoverOne` helper is now `public` so the IL path can reuse it without duplicating sender + authority recovery logic. The implementation is sound.\n\n---\n\n### Remaining issues\n\n#### Low\n\n**`catch (Exception)` without logging in `InclusionListDecoder.TryDecodeAndRecover:103`** → [discussion_r3318896887](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3318896887)\n\nAdded in `4a4f95b4`. A broad `catch (Exception)` with no logging silently swallows `OutOfMemoryException` and any future regression in `RecoverOne`. The comment explains the intent and the validator correctly handles the `null SenderAddress` outcome, so this is cosmetic — narrowing to expected exceptions (`CryptographicException | OverflowException | ArgumentException`) would be more precise.\n\n---\n\n**Verdict: mergeable.** All Critical, High, and Medium findings across all five review rounds are resolved. The one remaining Low item is a cosmetic exception-narrowing suggestion that does not affect correctness.","date":"2026-05-28T15:24:52Z"}
{"body":"**Claude finished @Marchhill's task in 4m 46s** —— [View job](https://github.com/NethermindEth/nethermind/actions/runs/26819067454)\n\n---\n### EIP-7805 (FOCIL) PR Review — Round 6\n\n**Tasks:**\n- [x] Load repo rules and context\n- [x] Read key changed files (new commits since Round 5)\n- [x] Check Round 5 Low finding (catch(Exception) narrowing in InclusionListDecoder)\n- [x] Review new commits for regressions or new issues\n- [x] Post inline comments\n- [x] Write summary\n\n---\n\n### Progress since Round 5\n\nThe Round 5 Low finding has been resolved cleanly (commit `62198665 IL decoder: narrow recovery catch + log at trace`):\n\n| Round 5 finding | Status |\n|---|---|\n| `catch (Exception)` without logging in `InclusionListDecoder.TryDecodeAndRecover` (Low) | ✅ Fixed — narrowed to `InvalidDataException \\| ArgumentException \\| CryptographicException`, logged at `Trace` |\n\n---\n\n### New commits reviewed\n\n| Commit | Summary | Issues |\n|---|---|---|\n| `62198665` | IL decoder: narrow recovery catch + log at trace | ✅ No issues (resolves Round 5 Low) |\n| `18e25aa6` | tidy: comment and test cleanup | ✅ No issues |\n| `0ed5ddff` | Cleanup: 2026 license, trim comments, drop dead code | ✅ No issues — essential rationale preserved, dead code from OptimismPlugin / BaseEngineModuleTests / MergePlugin / Ssz.Root removed cleanly |\n| `73fd89fa` | Drop last commented-out IBlockProcessor overload | ✅ No issues |\n| `2274531b` | Add equality logic; simplify tests (upstream) | ✅ No issues — doesn't touch FOCIL code |\n| `f718fe4c` | Merge upstream/master | ✅ Clean merge |\n\n---\n\n### Remaining issues\n\n#### Low\n\n**Stale comment in `PayloadPreparationService.cs:55`** → [discussion_r3341214440](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3341214440)\n\nThe comment on `_rebuildSlots` references `engine_updatePayloadWithInclusionListV1` (an earlier FOCIL draft endpoint that was never merged into the spec) and `ForceRebuildPayload` (a method that does not exist). The actual purpose of `_rebuildSlots` — tracking per-payload build count for test observability and storing parent header / attributes for improvement cycles — should be described instead.\n\n---\n\n**Verdict: mergeable.** All Critical, High, and Medium findings from Rounds 1–5 are resolved. The only remaining item is a Low-severity stale comment in `PayloadPreparationService.cs` that does not affect correctness or behaviour.","date":"2026-06-02T12:35:57Z"}
{"body":"**Claude finished @Marchhill's task in 4m 38s** —— [View job](https://github.com/NethermindEth/nethermind/actions/runs/26833801285)\n\n---\n### EIP-7805 (FOCIL) PR Review — Round 7\n\n**Tasks:**\n- [x] Load repo rules and context\n- [x] Read key changed files (new commits since Round 6)\n- [x] Check Round 6 Low finding (stale comment in PayloadPreparationService.cs)\n- [x] Review new commits for regressions or new issues\n- [x] Post inline comments\n- [x] Write summary\n\n---\n\n### Progress since Round 6\n\nThe Round 6 Low finding has been resolved cleanly (commit `3bd49e2b`):\n\n| Round 6 finding | Status |\n|---|---|\n| Stale comment in `PayloadPreparationService.cs` referencing `engine_updatePayloadWithInclusionListV1` and `ForceRebuildPayload` (Low) | ✅ Fixed — now describes what `_rebuildSlots` actually stores |\n\n---\n\n### New commits reviewed\n\n| Commit | Summary | Issues |\n|---|---|---|\n| `3bd49e2b` | PayloadPreparationService: fix stale comment | ✅ Resolves Round 6 Low |\n| `98e40f74` | Move IL satisfaction onto `IBlockValidator` | ✅ Clean architectural improvement; all wrappers updated |\n| `5c22ccc0` | NullBlockProcessor: restore `TransactionProcessed` event | ✅ No issues — restores missing no-op stub |\n| `e9e4fcf6` | Perf pass on FOCIL hot paths | ✅ Solid optimisations; see Low finding below |\n\nThe architectural refactor in `98e40f74` is clean: `IInclusionListValidator` is deleted, `ValidateInclusionList` is added to `IBlockValidator` next to the other consensus checks, and the static `InclusionListValidator.IsSatisfied` helper is retained for unit-test callability. `BlockchainProcessor` reads `block.InclusionListUnsatisfied` directly rather than calling back through `IBlockProcessor`. All wrappers (`Always`, `SimulateBlockValidatorProxy`, `InvalidBlockInterceptor`, `BalRecordingBlockValidator`, `TestBlockValidator`) are updated.\n\nThe perf optimisations in `e9e4fcf6` are valid:\n- Reservoir sampling (`Algorithm R + Fisher-Yates`) avoids materialising the full filtered mempool into the IL output buffer — O(N=MaxTxs) memory, single pass over the mempool array.\n- Incremental `KeccakHash` eliminates the 8 KiB concat buffer in `ComputeInclusionListDigest`.\n- `CollectionsMarshal.GetValueRefOrAddDefault` halves the hash-table probes in `CaptureParentSenderState` and `IsSatisfied`.\n\n---\n\n### Remaining issues\n\n#### Low\n\n**Stackalloc threshold is 128, but `MaxTransactionsPerInclusionList` is 256** → [discussion_r3342860263](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3342860263)\n\nThe bitmap comment says \"(currently impossible) \u003e128 case\" but `MaxTransactionsPerInclusionList = MaxBytesPerInclusionList / MinTransactionSizeBytesLower = 8192 / 32 = 256`. A full-capacity IL (129–256 txs) will heap-allocate. Since 256 × 1 byte = 256 bytes on the stack, the threshold should be raised to 256.\n\n**Dead stub method in `BalRecordingBlockProcessorTests.cs:70`** → [discussion_r3342860546](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3342860546)\n\n`ValidateInclusionList(Block suggestedBlock, Block block, ProcessingOptions options)` was removed from `IBlockProcessor` in commit `98e40f74` but the matching stub in `StubBlockProcessor` was not cleaned up. It is dead code.\n\n---\n\n**Verdict: mergeable.** All Critical, High, and Medium findings across Rounds 1–6 are resolved. The two remaining Low items are cosmetic (comment accuracy, dead test stub) and do not affect correctness, consensus, or security.","date":"2026-06-02T16:34:37Z"}
{"body":"**Claude finished @Marchhill's task in 5m 53s** —— [View job](https://github.com/NethermindEth/nethermind/actions/runs/26889065792)\n\n---\n### EIP-7805 (FOCIL) PR Review — Round 8\n\n**Tasks:**\n- [x] Load repo rules and context\n- [x] Read key changed files (new commits since Round 7)\n- [x] Check Round 7 Low findings\n- [x] Post inline comments\n- [x] Write summary\n\n---\n\n### Progress since Round 7\n\nBoth Round 7 Low findings are resolved cleanly (commit `5b933956`):\n\n| Round 7 finding | Status |\n|---|---|\n| Stackalloc threshold 128 \u003c `MaxTransactionsPerInclusionList` 256 (Low) | ✅ Fixed — threshold raised to `Eip7805Constants.MaxTransactionsPerInclusionList` |\n| Dead `ValidateInclusionList` stub in `BalRecordingBlockProcessorTests.StubBlockProcessor` (Low) | ✅ Removed |\n\n---\n\n### New commits reviewed\n\n| Commit | Summary | Issues |\n|---|---|---|\n| `5b933956` | Drop orphan IL stub + raise stackalloc threshold | ✅ Resolves Round 7 Lows |\n| `a0cfe7f5` | tidy | ✅ No issues — removes now-superseded comments and dead code |\n| `d1a2b167` | Encapsulate IL validation lifecycle behind `IBlockValidator` | ✅ Clean architectural refactor; two-phase `BeginInclusionListValidation`/`Commit` moves IL plumbing off `BlockProcessor` |\n| `84df88dd` | Doc: `BeginInclusionListValidation` two-phase contract | ✅ Documentation only |\n| `43dfec6a` | Revert IL satisfaction check to post-execution state | ⚠️ See Medium finding below |\n\n---\n\n### Remaining issues\n\n#### Medium\n\n**IL satisfaction uses post-execution state — spec citation needed (third reversal)** → [discussion_r3349038010](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3349038010)\n\nCommit `43dfec6a` reverts `IsSatisfied` from the pre-execution (parent state + included IL txs) approach implemented in Rounds 3–4 back to fully post-execution state (all block transactions applied). The commit message claims this is what the spec requires. The test `Same_nonce_replacement_tx_advances_nonce_and_satisfies_il` explicitly documents the consequence: a builder who includes a same-nonce replacement tx from an IL sender can advance that sender's post-execution nonce, making the original IL tx appear non-appendable, and the block is accepted.\n\nIf the EIP-7805 spec genuinely uses post-execution state for the appendability check, this is correct and my Rounds 2–4 analysis was wrong. Given this is the third position change on the same question, a direct spec reference (EIP-7805 §Execution Layer or [execution-apis PR #609](https://github.com/ethereum/execution-apis/pull/609)) confirming post-execution semantics is needed before merge to close the loop definitively.\n\n#### Low\n\n**Stale doc comment on `engine_newPayloadV6`** → [discussion_r3349036216](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3349036216)\n\n`EngineRpcModule.Bogota.cs:29–32` still says \"validated against the **parent-block state** snapshot\" — this was updated in Round 7 to match the then-current pre-execution implementation, but after `43dfec6a` reverted to post-execution, the comment is wrong again. One-line suggestion attached to the inline comment.\n\n---\n\n**Verdict: not mergeable.** The Medium finding (spec citation for post-execution IL semantics) needs to be resolved: either add a direct spec reference confirming post-execution is correct, or revert to the pre-execution approach if the spec text says otherwise. The Low (stale doc) is a one-line fix.","date":"2026-06-03T13:48:26Z"}
{"body":"**Claude finished @Marchhill's task in 5m 9s** —— [View job](https://github.com/NethermindEth/nethermind/actions/runs/26972555106)\n\n---\n### EIP-7805 (FOCIL) PR Review — Round 9\n\n**Tasks:**\n- [x] Load repo rules and context\n- [x] Read key changed files (new commits since Round 8)\n- [x] Check Round 8 findings (stale doc comment Low, spec citation Medium)\n- [x] Review new commits for regressions or new issues\n- [x] Write summary\n\n---\n\n### Progress since Round 8\n\nBoth Round 8 findings are resolved:\n\n| Round 8 finding | Status |\n|---|---|\n| Post-execution state for IL check: spec citation needed (Medium) | ✅ Closed — Marchhill confirmed post-execution is correct per [EIP-7805 §Execution Layer](https://eips.ethereum.org/EIPS/eip-7805#execution-layer); doc comment updated in `d6d444b0` |\n| Stale doc comment in `EngineRpcModule.Bogota.cs` \"parent-block state snapshot\" (Low) | ✅ Fixed in `d6d444b0` — now reads \"post-execution state\" |\n\n---\n\n### New commits reviewed\n\n| Commit | Summary | Issues |\n|---|---|---|\n| `d6d444b0` | Refactor: throw `InclusionListUnsatisfiedException` instead of flag | ✅ Clean — removes mutable `InclusionListUnsatisfied` flag from `Block.cs`; exception correctly propagates through `ProcessBranch` (which only catches `InvalidBlockException`) up to `ProcessBlocks`; `InclusionListUnsatisfiedException : BlockchainException` is a sibling of `InvalidBlockException`, not a subtype, so no accidental catch-widening |\n| `96e72c1a` | Remove commented-out specprovider test | ✅ No issues |\n| `02fdfd11` | Use `ArrayPoolList` for oversized IL bitmap and reservoir sampler | ✅ `new ArrayPoolList\u003cbool\u003e(il.Length, il.Length)` zeroes the rented buffer via `AsSpan(0, startingCount).Clear()` before use; `ReduceCount` correctly trims null tail entries; `try-finally` in `DecodeTransactionsUpToLimit` guarantees `ArrayPoolList\u003cTransaction\u003e` disposal even on early exit or exception via enumerator `Dispose()` |\n| `11bdfcfa` | Tidy test comment | ✅ No issues |\n| `672b1955` | Simplify `IEngineRpcModule.Bogota.cs` JSON-RPC descriptions | ✅ No issues |\n| `5b358b9e` + `f744cbc1` | Tidy/whitespace in `MergePlugin.cs` | ✅ No issues |\n\n---\n\n**Verdict: mergeable.** All Critical, High, and Medium findings across all nine review rounds are resolved. No new issues found in the latest commits.","date":"2026-06-04T18:50:13Z"}
{"body":"**Claude finished @Marchhill's task in 9m 29s** —— [View job](https://github.com/NethermindEth/nethermind/actions/runs/27010678229)\n\n---\n### EIP-7805 (FOCIL) PR Review — Round 10\n\n**Tasks:**\n- [x] Load repo rules and context\n- [x] Read new commits since Round 9\n- [x] Check for any new issues\n- [x] Post inline comments\n- [x] Write summary\n\n---\n\n### Progress since Round 9\n\nRound 9 reviewed up to `f744cbc1`. The following commits have landed since then (`daf80efe` was present at Round 9 but not listed in that review's table):\n\n| Commit | Summary | Issues |\n|---|---|---|\n| `daf80efe` | cleanup and optimise: raise parallel threshold to 16, use `ArrayPoolList` for slots, remove `_rebuildSlots` / `GetPayloadBuildCount` from `PayloadPreparationService` | ✅ No issues |\n| `c23c8ef1` | optimisations: O(N+M) IL hash-lookup, sender-state dedup cache, `Random.Shared` in `InclusionListBuilder` | ✅ No issues |\n| `42de652c` | reduce allocations: plain `Transaction[]` slots (in-place compact), `Transaction[]` reservoir, drop `ArrayPoolList` in `InclusionListValidator` | ✅ No issues |\n| `53059f63` | simplify IL decoding: `InclusionListDecoder.DecodeAndRecover` delegates entirely to `TxsDecoder.DecodeTxs` + `RecoverSignatures.RecoverData(skipErrors: true)` | ⚠️ See Low finding |\n| `4022f386` | remove `RecoverOne` (no longer needed after `53059f63`) | ✅ No issues |\n| `99df16c3` | txsdecoder reuse: `ExecutionPayload.TryGetTransactions` now uses `TxsDecoder.DecodeTxs`; `InclusionListTxSource` reverts to lazy decoder init (avoids null-dep throw in test stubs) | ✅ No issues |\n\nThe `53059f63` simplification is a meaningful improvement: it removes ~90 lines of custom parallel decode/recover logic from `InclusionListDecoder` and reuses the battle-tested `TxsDecoder` + `RecoverSignatures` infrastructure that serves normal block transactions.\n\n---\n\n### Remaining issues\n\n#### Low\n\n**`skipErrors: false` for IL-tx recovery in the block preprocessor** → [discussion_r3362195537](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3362195537)\n\n`ExecutionPayloadV3.TryGetBlock()` decodes IL transactions via `TxsDecoder.DecodeTxs(..., skipErrors: true)` — decode-only, no sender recovery — so all IL txs enter block processing with `SenderAddress = null`. The block preprocessor (`RecoverSignatures.RecoverData(Block)`) then calls `RecoverData(block.InclusionListTransactions, spec)` with `skipErrors` defaulting to `false`. Because `txs[0].SenderAddress is null`, the early-return optimisation does not fire and recovery is attempted for every IL tx.\n\nFor a well-formed IL this is harmless. For an adversarially crafted `engine_newPayloadV6` with an IL tx that has valid RLP but an invalid signature, ECDSA recovery throws, block processing fails with an exception, and the CL receives `UnknownBlockError` instead of a clean `INVALID` status. The `skipErrors` parameter added in `53059f63` was purpose-built for this case; it just wasn't applied to this call site:\n\n```csharp\nRecoverData(block.InclusionListTransactions, spec, skipErrors: true);\n```\n\n---\n\n**Verdict: mergeable.** All Critical, High, and Medium findings from Rounds 1–9 remain resolved. The sole remaining Low item is a one-word fix that converts an adversarial-input exception path into a clean `INVALID` response.","date":"2026-06-05T10:52:27Z"}
{"body":"**Claude finished @Marchhill's task in 3m 54s** —— [View job](https://github.com/NethermindEth/nethermind/actions/runs/27016175146)\n\n---\n### EIP-7805 (FOCIL) PR Review — Round 11\n\n**Tasks:**\n- [x] Load repo rules and context\n- [x] Read new commits since Round 10\n- [x] Verify Round 10 Low finding is resolved (`skipErrors: true` for IL recovery)\n- [x] Review new commits for regressions or new issues\n- [x] Write summary\n\n---\n\n### Progress since Round 10\n\nThe Round 10 Low finding has been resolved cleanly (commit `f737c3d4`):\n\n| Round 10 finding | Status |\n|---|---|\n| `skipErrors: false` for IL-tx recovery in the block preprocessor (Low) | ✅ Fixed — `RecoverData(block.InclusionListTransactions, spec, skipErrors: true)` with `catch` narrowed to `InvalidDataException \\| ArgumentException \\| CryptographicException`, logged at `Trace` |\n\n---\n\n### New commits reviewed\n\n| Commit | Summary | Issues |\n|---|---|---|\n| `1df99cf0` | Intermediate fix: revert `TryGetTransactions` to serial-inline after ZK_EVM stateless test failures | ✅ No issues (superseded by `f79494c5`) |\n| `f79494c5` | Gate `TxsDecoder` parallel branch behind `#if !ZK_EVM`; re-dedup `TryGetTransactions` onto `TxsDecoder.DecodeTxs` | ✅ No issues — ZK stub returns `false`, causing `DecodeTxs` to fall back to `DecodeSequential`; stateless flavor retains no `System.Threading.Tasks` reference |\n| `1a871be7` | Collapse to a single `#if/#else` by extracting `DecodeSequential` as a named, unconditional method | ✅ Clean structural refactor — `ParallelDecodeThreshold` const and `TryDecodeParallel` live inside the same `#if !ZK_EVM` block, keeping the ZK stub minimal |\n| `f737c3d4` | `RecoverData`: pass `skipErrors: true` for IL txs in the block preprocessor | ✅ Resolves Round 10 Low — adversarial IL tx with valid RLP but bad signature now leaves `SenderAddress = null` rather than surfacing a JSON-RPC error |\n\nThe `TryDecodeParallel` implementation is correct on both error paths: `!skipErrors` + error → stores lowest-index error via `lock(errorGate)`, calls `state.Stop()`, returns the error string (null slots never observed); `skipErrors` + errors → exceptions caught per-worker, null slots compacted in a final sequential pass before returning.\n\n---\n\n**Verdict: mergeable.** All Critical, High, and Medium findings across all 11 review rounds are resolved. No new issues found in the latest commits.","date":"2026-06-05T12:57:31Z"}
{"body":"**Claude finished @Marchhill's task in 5m 21s** —— [View job](https://github.com/NethermindEth/nethermind/actions/runs/27147107283)\n\n---\n### EIP-7805 (FOCIL) PR Review — Round 12\n\n**Tasks:**\n- [x] Load repo rules and context\n- [x] Read new commits since Round 11\n- [x] Check for any new issues\n- [x] Post inline comments\n- [x] Write summary\n\n---\n\n### Progress since Round 11\n\nRound 11 reviewed up to `f737c3d4`. New commits:\n\n| Commit | Summary | Issues |\n|---|---|---|\n| `b69ea5e1` | tidy, optimise encode — `EncodePooled` zero-alloc path via `UnsafeGetInternalArray` + `CappedArray` | ⚠️ See Low finding |\n| `08b58ad6` | optimise GetInclusionList — introduce `InclusionListBytes` wrapper (`IReadOnlyList\u003cArrayPoolList\u003cbyte\u003e\u003e`, `IDisposable`); `EncodeTransactionsUpToLimit` now returns it; `GetInclusionListTransactionsHandler` updated | ✅ No issues |\n| `f4a916d1` | missing file — adds `InclusionListBytes.cs` | ✅ No issues |\n| `cf03bb6a` | tidy — remove 5-line comment block from `InclusionListDecoder` | ✅ No issues |\n| `a0fb199d` | fix lint — remove unused `using` in 7 files | ✅ No issues |\n| `404e52f1` | tidy comments — remove comments from `PayloadAttributesPayloadIdTests` and `InclusionListBytes` | ✅ No issues |\n| `502d43ae` | add more tests — 4 new `ExecutionPayloadTests` covering serial/parallel decode boundary, parallel-matches-serial oracle, stability across 50 iterations, and 8-thread concurrent-caller stress | ✅ No issues |\n\nThe `InclusionListBytes` design is sound:\n- `Dispose()` calls `_items.DisposeRecursive()` which disposes each inner `ArrayPoolList\u003cbyte\u003e` (returning pool memory) then disposes the outer list — correct disposal chain.\n- `EncodePooled` pre-sizes an `ArrayPoolList\u003cbyte\u003e` with `startingCount = length`, wraps `UnsafeGetInternalArray()` in a `CappedArray\u003cbyte\u003e(array, length)` so `RlpStream` writes exactly `length` bytes — correctly avoids exposing over-allocated pool memory.\n- `ResultWrapper\u003cInclusionListBytes\u003e.DisposePayloads()` → `DisposeIfReferenceType(Data)` → `TryDispose()` → `InclusionListBytes.Dispose()` — pool memory is returned after the response is written.\n- Serializes as `[\"0xRLP...\", ...]` via the registered `ArrayPoolListByteHexConverter` — correct for the EIP-7805 `engine_getInclusionListV1` response format.\n\n---\n\n### Remaining issues\n\n#### Low\n\n**`\u003c/summary\u003e` tag missing in `IBlockValidator.cs:18`** → [discussion_r3374321800](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3374321800)\n\nCommit `b69ea5e1` removed the closing line of the summary but left the opening `\u003csummary\u003e` tag open. Will produce an XML doc parser warning and truncate IntelliSense tooltips. One-line suggestion attached.\n\n---\n\n**Verdict: mergeable.** All Critical, High, and Medium findings across all 12 review rounds are resolved. The sole remaining item is a Low-severity doc comment issue (missing `\u003c/summary\u003e`) that does not affect correctness, consensus, or security.","date":"2026-06-08T15:08:10Z"}
{"body":"**Claude finished @Marchhill's task in 3m 17s** —— [View job](https://github.com/NethermindEth/nethermind/actions/runs/27148734883)\n\n---\n### EIP-7805 (FOCIL) PR Review — Round 13\n\n**Tasks:**\n- [x] Load repo rules and context\n- [x] Read new commits since Round 12\n- [x] Verify Round 12 Low finding (`\u003c/summary\u003e` tag missing in `IBlockValidator.cs`)\n- [x] Check for any new issues\n- [x] Write summary\n\n---\n\n### Progress since Round 12\n\nThe Round 12 Low finding has been resolved cleanly (commit `fcfbf783`):\n\n| Round 12 finding | Status |\n|---|---|\n| `\u003c/summary\u003e` tag missing in `IBlockValidator.cs:18` after `b69ea5e1` tidy (Low) | ✅ Fixed — closing tag restored |\n\n---\n\n### New commits reviewed\n\n| Commit | Summary | Issues |\n|---|---|---|\n| `08b58ad6` | Optimise `GetInclusionList` — introduce `InclusionListBytes` wrapper (`IReadOnlyList\u003cArrayPoolList\u003cbyte\u003e\u003e`, `IDisposable`); `EncodeTransactionsUpToLimit` returns it; handler updated | ✅ No issues |\n| `f4a916d1` | Add missing `InclusionListBytes.cs` file | ✅ No issues |\n| `cf03bb6a` | Tidy — remove comment block from `InclusionListDecoder` | ✅ No issues |\n| `a0fb199d` | Fix lint — remove unused `using` directives in 7 files | ✅ No issues |\n| `404e52f1` | Tidy comments — remove stale comments from `PayloadAttributesPayloadIdTests` and `InclusionListBytes` | ✅ No issues |\n| `502d43ae` | Add more tests — 4 new `ExecutionPayloadTests`: serial/parallel decode boundary, parallel-matches-serial oracle, stability across 50 iterations, 8-thread concurrent-caller stress | ✅ No issues |\n| `fcfbf783` | Fix comment — restore `\u003c/summary\u003e` tag in `IBlockValidator.cs` | ✅ Resolves Round 12 Low |\n\nThe `InclusionListBytes` design is correct:\n- `Dispose()` chains through `_items.DisposeRecursive()` returning all rented `ArrayPoolList\u003cbyte\u003e` items to the pool\n- `EncodeTransactionsUpToLimit` correctly disposes skipped (oversized) `txBytes` inline via `txBytes.Dispose()` before `continue`\n- The `ResultWrapper\u003cInclusionListBytes\u003e` disposal path from `DisposePayloads()` → `TryDispose(Data)` → `InclusionListBytes.Dispose()` is correct\n\nThe new `ExecutionPayloadTests` are well-designed:\n- The parallel-matches-serial oracle test catches any field-mixup from thread-unsafe decoder state\n- The 50-iteration stability test catches non-deterministic order drift\n- The 8-thread concurrent-caller stress test (`Task.Run` × 8, 20 iterations each) catches static shared-state bugs in per-type decoders\n\n---\n\n**Verdict: mergeable.** All Critical, High, and Medium findings across all 13 review rounds are resolved. No new issues found in the latest commits.","date":"2026-06-08T15:37:49Z"}
{"body":"**Claude finished @Marchhill's task in 5m 42s** —— [View job](https://github.com/NethermindEth/nethermind/actions/runs/27205099522)\n\n---\n### EIP-7805 (FOCIL) PR Review — Round 14\n\n**Tasks:**\n- [x] Load repo rules and context\n- [x] Read new commits since Round 13\n- [x] Check LukaszRozmej's new review findings\n- [x] Review key changed files\n- [x] Post inline comments\n- [x] Write summary\n\n---\n\n### Progress since Round 13\n\nOne commit since Round 13 (`60068afa review comments`) addressed a significant batch of LukaszRozmej's findings:\n\n| LukaszRozmej finding | Status |\n|---|---|\n| Blob-tx exclusion in `IsSatisfied` (`tx.SupportsBlobs`) [Must-fix] | ✅ Fixed — `if (tx.SupportsBlobs) return false;` + test |\n| EIP-3607 / code-at-sender check [Must-fix] | ✅ Fixed — `HasCode \u0026\u0026 !IsDelegatedCode` guard + two tests |\n| Duplicate IL entries use `TryAdd` [Must-fix] | ✅ Fixed — first occurrence preserved; post-state nonce catches the duplicate; test added |\n| `null` IL returns `false` inconsistency | ✅ Fixed — `IsSatisfied` returns `true` on null (non-engine-API path) |\n| Sender-cache miss on non-existent accounts | ✅ Fixed — `TryGetAccount` result ignored, default `AccountStruct` always cached |\n| Silent-pass on null IL in `BlockValidator` | ✅ Fixed — early null guard removed; `IsSatisfied` handles it |\n| Reservoir allocation always 256 slots | ✅ Fixed — `Math.Min(mempool.Length, capacity)` |\n\n---\n\n### Remaining issues\n\n#### Medium\n\n**Payload-id digest collision on empty IL entries** → [discussion_r3380541613](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3380541613)\n\n`ComputeInclusionListDigest` skips `null`/empty entries without a length prefix. This means `[\u003cempty\u003e, tx1]` and `[tx1]` hash to the same digest → same `payload_id`. A CL that sends two FCUs with structurally-different ILs that happen to collide will receive a payload built for the wrong IL configuration. The fix is to either length-prefix each entry in the hash, or reject empty entries upstream in the V5 payload-attributes validation.\n\n#### Low\n\n- **`IndexOutOfRangeException` in FCU catch** → [discussion_r3380542577](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3380542577) — `RlpException` already covers legitimate malformed input from that path; catching `IndexOutOfRangeException` hides genuine encoder bugs.\n- **Dead `CancelAndDispose(this CancellationTokenSource)` extension** → [discussion_r3380543385](https://github.com/NethermindEth/nethermind/pull/8003#discussion_r3380543385) — added in this PR, never called; existing callers use `SharedCancellationTokenSource`'s instance method.\n- **`MaxTransactionsPerInclusionList = 256` is theoretical** — `Eip7805Constants.cs` explains `8192 / 32` but not that real-world minimum tx size is ~100 bytes; a short comment here would help future readers who might use `256` as a \"real\" protocol cap.\n- **`init` setters on `ExecutionRequests`/`InclusionListTransactions` in `ExecutionPayloadParams`** — both are only assigned in the constructor; `init` would prevent accidental mutation after `ValidateParams`.\n\n---\n\n**Verdict: not yet mergeable.** The payload-id digest collision (Medium) is the only blocking item — it's LukaszRozmej's [Must-fix] that wasn't addressed in the latest commit. The three Low items and two Polish items do not block merge.","date":"2026-06-09T12:20:16Z"}
{"body":"**Claude finished @Marchhill's task in 2m 42s** —— [View job](https://github.com/NethermindEth/nethermind/actions/runs/27222196475)\n\n---\n### EIP-7805 (FOCIL) PR Review — Round 15\n\n**Tasks:**\n- [x] Load repo rules and context\n- [x] Read new commits since Round 14\n- [x] Check Round 14 findings\n- [x] Review new commits for regressions or new issues\n- [x] Write summary\n\n---\n\n### Progress since Round 14\n\nTwo commits since the Round 14 review (`60068afa`):\n\n| Commit | Summary | Issues |\n|---|---|---|\n| `30696bb3` | More review comments: digest fix, drop `IndexOutOfRangeException`, remove `CancelAndDispose` | ✅ Resolves Round 14 Medium + two Lows |\n| `3dfe25ca` | Add comments: `RlpException` in `skipErrors` catch, `BlockValidator` null-IL comment, `Eip7805Constants` bound comment, `InclusionListBytes` dispose comment | ✅ Resolves Round 14 Low; `RlpException` addition is correct |\n| `614d90ff` | Merge `upstream/master` | ✅ Chain config / CI updates only; no FOCIL code |\n\n---\n\n### Round 14 findings resolved\n\n| Round 14 finding | Status |\n|---|---|\n| Payload-id digest collision on empty/null IL entries (Medium) | ✅ Fixed — `ComputeInclusionListDigest` now 4-byte-length-prefixes every entry; `[empty, tx1]` and `[tx1]` now hash differently |\n| `IndexOutOfRangeException` in FCU catch list (Low) | ✅ Removed — `RlpException` already covers legitimate malformed input |\n| Dead `CancelAndDispose` extension (Low) | ✅ Removed |\n| `MaxTransactionsPerInclusionList = 256` is theoretical (Low) | ✅ Comment added explaining the stackalloc-bound intent and real-world practical maximum |\n| `init` setters on payload-param properties (Polish) | Not changed — Marchhill confirmed `InclusionListTransactions` is mutated in `TaikoExecutionPayload`; `set;` is correct |\n\nThe `RlpException` addition to the `skipErrors` catch in `RecoverSignatures` (parallel and serial paths) is sound: ECDSA recovery for EIP-7702 authorization tuples may parse sub-encoded fields, so catching `RlpException` alongside `CryptographicException | ArgumentException | InvalidDataException` is a reasonable defensive widening under `skipErrors: true`.\n\n---\n\n**Verdict: mergeable.** All Critical, High, and Medium findings across all 15 review rounds are resolved. No new issues found in the latest commits.","date":"2026-06-09T16:57:42Z"}
